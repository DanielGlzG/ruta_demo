name: ruta_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  cloud_firestore: ^5.4.2
  connectivity_plus: ^6.0.5
  dio: ^5.7.0
  dropdown_search: ^6.0.1
  equatable: ^2.0.5
  firebase_core: ^3.5.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  path_provider: ^2.1.4
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/jsons/articulos.json
    - .env
